@model IList<ProductOverviewModel>
@if (Model.Count > 0)


{<div class="collaps-filter">

        <h2>@T("Products.RecentlyViewedProducts")</h2>
        <div class="brands-name filter-content">
            @for (int i = 0; i < Model.Count; i++)
            {
                var product = Model[i];
                <div class="media">
                    @if (!String.IsNullOrEmpty(product.PictureModels.FirstOrDefault()?.ImageUrl))
                    {
                        <div class="media-left media-middle">
                            <a class="product-picture" href="@Url.RouteUrl("Product", new { SeName = product.SeName })" title="@product.PictureModels.FirstOrDefault()?.Title">
                                <img class="img-thumbnail" alt="@product.PictureModels.FirstOrDefault()?.AlternateText" src="@product.PictureModels.FirstOrDefault()?.ImageUrl" title="@product.PictureModels.FirstOrDefault()?.Title" />
                            </a>
                        </div>
                    }
                    <div class="media-body">
                        <a class="product-name" href="@Url.RouteUrl("Product", new { SeName = product.SeName })">@product.Name</a>
                    </div>
                </div>
            }
        </div>
    </div>

}